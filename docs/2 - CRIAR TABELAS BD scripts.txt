CREATE TABLE PRODUTOR
(CODPRODUTOR NUMBER(10),
 CPFCNPJ VARCHAR2(18),
 NOME VARCHAR2(60),
 DTCADASTRO DATE,
 DTULTALTER DATE,
CONSTRAINT PRODUTOR_PK PRIMARY KEY (CODPRODUTOR, CPFCNPJ))
/
CREATE SEQUENCE PRODUTOR_DFSEQ
/
CREATE TABLE DISTRIBUIDOR
(CODDISTRIB NUMBER(10),
 CNPJ VARCHAR2(18),
 NOME VARCHAR2(60),
 DTCADASTRO DATE,
 DTULTALTER DATE,
CONSTRAINT DISTRIBUIDOR_PK PRIMARY KEY (CODDISTRIB, CNPJ))
/
CREATE SEQUENCE DISTRIBUIDOR_DFSEQ
/
CREATE TABLE LIMITECREDITO
(CODPRODUTOR NUMBER(10),
 CODDISTRIB NUMBER(10),
 LIMITECRED NUMBER(12,2), 
CONSTRAINT LIMITECREDITO_PK PRIMARY KEY (CODPRODUTOR, CODDISTRIB))
/
CREATE TABLE PRODUTO
(CODPROD NUMBER(10),
 NOME VARCHAR2(60),
 PRECO NUMBER(18,6),
 DTCADASTRO DATE,
 DTULTALTER DATE,
CONSTRAINT PRODUTO_PK PRIMARY KEY (CODPROD))
/
CREATE SEQUENCE PRODUTO_DFSEQ
/
CREATE TABLE NEGOCIACAO
(NUMNEGOC NUMBER(10),
 CODPRODUTOR NUMBER(10),
 CODDISTRIB NUMBER(10),
 CODPROD NUMBER(10),
 SEQ NUMBER(4),
 PRECO NUMBER(18,6),
 QT NUMBER(18,6),
 STATUS VARCHAR2(1),
 DTEMISSAO DATE,
 DTAPROVACAO DATE,
 DTCONCLUSAO DATE,
 DTCANCEL DATE,
CONSTRAINT NEGOCIACAO_PK PRIMARY KEY (NUMNEGOC, CODPROD, SEQ))
/
CREATE SEQUENCE NEGOCIACAO_DFSEQ